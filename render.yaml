services:
- type: web
  name: bifinance-bot
  env: python
  plan: free
  buildCommand: pip install -r requirements.txt
  startCommand: python main.py
  envVars:
  - key: BOT_TOKEN
    sync: false
  - key: PORT
    value: 10000
